# Configure Nixpacks to use our startup script
[phases.setup]
nixPkgs = ["nodejs_18", "bash", "curl"]

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = [
    "npx prisma generate",
    "npm run build"
]

[start]
cmd = "bash start.sh"
