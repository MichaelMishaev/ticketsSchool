[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Disable healthcheck - just check if container is running
startCommand = "bash start.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
