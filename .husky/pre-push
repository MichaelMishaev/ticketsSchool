echo "🧪 Running P0 critical tests before push..."
echo ""
echo "⚠️  This may take 1-2 minutes. You can skip with --no-verify if needed."
echo ""

# Run P0 critical tests only (faster than full suite)
npm run test:p0
if [ $? -ne 0 ]; then
  echo ""
  echo "❌ P0 critical tests failed!"
  echo ""
  echo "Please fix failing tests before pushing to remote."
  echo "To bypass this check (emergency only): git push --no-verify"
  exit 1
fi

echo ""
echo "✅ All P0 critical tests passed!"
echo "🚀 Proceeding with push..."
