# Behavior Lock Test Execution Flow

## Visual Test Flow

```
┌─────────────────────────────────────────────────────────────┐
│  Behavior Lock Test Suite                                   │
│  16 Tests | ~30 seconds | API-based (fast)                  │
└─────────────────────────────────────────────────────────────┘
                           │
                           ▼
        ┌──────────────────────────────────────┐
        │  1. API Sort Order Locks (2 tests)   │
        └──────────────────────────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                     │
        ▼                                     ▼
┌───────────────────┐              ┌────────────────────┐
│ Events sorted by  │              │ Registrations      │
│ createdAt DESC    │              │ sorted by          │
│ (newest first)    │              │ createdAt ASC      │
└───────────────────┘              └────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  2. Multi-Tenant Isolation Locks (4 tests)           │
│  ⚠️  CRITICAL SECURITY                                │
└──────────────────────────────────────────────────────┘
        │
        ├─── Non-SuperAdmin auto-filter by schoolId
        ├─── SuperAdmin can see all schools
        ├─── Cross-school access → 403 Forbidden
        └─── Admin without school → 403 + error message
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  3. Soft Delete Behavior Locks (1 test)              │
└──────────────────────────────────────────────────────┘
        │
        └─── CANCELLED registrations excluded from count
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  4. API Response Shape Locks (3 tests)               │
└──────────────────────────────────────────────────────┘
        │
        ├─── GET /api/events response shape
        ├─── POST /api/events returns full object
        └─── Error responses: {error: string} format
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  5. Permission Boundary Locks (4 tests)              │
└──────────────────────────────────────────────────────┘
        │
        ├─── ADMIN can create events
        ├─── MANAGER view-only (403 on create)
        ├─── VIEWER read-only (403 on create)
        └─── SUPER_ADMIN full access
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  6. Side Effect Locks (2 tests)                      │
│  ⚠️  HIGH RISK - Atomic operations                    │
└──────────────────────────────────────────────────────┘
        │
        ├─── Create registration → increment spotsReserved
        └─── Cancel registration → decrement spotsReserved
        │
        ▼
┌──────────────────────────────────────────────────────┐
│  ✅ All 16 Tests Pass                                │
│  Behaviors Locked & Protected from Silent Changes    │
└──────────────────────────────────────────────────────┘
```

## Test Execution Timeline

```
0s    ────► Test Suite Starts
      │
2s    ├──► API Sort Order Locks (parallel)
      │    ├─ Events DESC ✓
      │    └─ Registrations ASC ✓
      │
6s    ├──► Tenant Isolation Locks (parallel)
      │    ├─ Auto-filter schoolId ✓
      │    ├─ SuperAdmin bypass ✓
      │    ├─ Cross-school 403 ✓
      │    └─ No school 403 ✓
      │
10s   ├──► Soft Delete Locks
      │    └─ CANCELLED excluded ✓
      │
14s   ├──► Response Shape Locks (parallel)
      │    ├─ GET shape ✓
      │    ├─ POST shape ✓
      │    └─ Error shape ✓
      │
20s   ├──► Permission Boundary Locks (parallel)
      │    ├─ ADMIN create ✓
      │    ├─ MANAGER view-only ✓
      │    ├─ VIEWER read-only ✓
      │    └─ SUPER_ADMIN full ✓
      │
26s   ├──► Side Effect Locks (sequential - atomic)
      │    ├─ Create increment ✓
      │    └─ Cancel decrement ✓
      │
30s   ────► Test Suite Complete ✅
```

## Data Flow Per Test

```
┌──────────────────┐
│ Test Setup       │
│ - Create school  │
│ - Create admin   │
│ - Create event   │
│ - Login          │
│ - Get session    │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ API Call         │
│ GET /api/events  │
│ + session cookie │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Assertion        │
│ EXACT behavior   │
│ .toEqual()       │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│ Cleanup          │
│ (afterAll hook)  │
└──────────────────┘
```

## Parallel vs Sequential Execution

### Parallel Tests (Faster)
```
┌─────────┐  ┌─────────┐  ┌─────────┐
│ Test 1  │  │ Test 2  │  │ Test 3  │
│ School A│  │ School B│  │ School C│
└────┬────┘  └────┬────┘  └────┬────┘
     │            │            │
     └────────────┴────────────┘
              │
              ▼
         All complete
```

### Sequential Tests (Atomic operations)
```
┌─────────┐
│ Test 1  │
│ Create  │
└────┬────┘
     │
     ▼
┌─────────┐
│ Test 2  │
│ Cancel  │
└────┬────┘
     │
     ▼
 Complete
```

## Failure Detection Flow

```
Test Executes
     │
     ▼
  Passes? ──YES──► Continue
     │
     NO
     │
     ▼
┌─────────────────────────────┐
│ Behavior Lock Failure       │
│ ⚠️  Silent Regression Alert │
└──────────┬──────────────────┘
           │
           ▼
    Was change intentional?
     │              │
     NO             YES
     │              │
     ▼              ▼
┌─────────┐    ┌─────────────┐
│ REVERT  │    │ UPDATE TEST │
│ CHANGES │    │ + DOCUMENT  │
└─────────┘    └─────────────┘
```

## Integration with CI/CD

```
GitHub PR Created
     │
     ▼
┌─────────────────┐
│ CI/CD Pipeline  │
└────────┬────────┘
         │
         ├──► Unit Tests
         ├──► P0 Feature Tests
         ├──► Behavior Lock Tests ◄── NEW
         ├──► Security Tests
         └──► Build & Deploy
              │
              ▼
         All Pass? ──NO──► ❌ Block Deploy
              │
              YES
              │
              ▼
         ✅ Deploy Safe
```

## Test Dependencies

```
behavior-locks.spec.ts
    │
    ├──► fixtures/test-data.ts
    │    ├─ createSchool()
    │    ├─ createAdmin()
    │    ├─ createEvent()
    │    ├─ createRegistration()
    │    └─ cleanupTestData()
    │
    ├──► page-objects/LoginPage.ts
    │    ├─ goto()
    │    └─ login()
    │
    └──► helpers/test-helpers.ts
         └─ generateEmail()
```

## Critical Path Tests (Must Always Pass)

```
🔴 CRITICAL SECURITY (4 tests)
    ├─ Multi-tenant isolation
    ├─ Cross-school access rejection
    ├─ Admin without school error
    └─ Atomic capacity operations

🟡 HIGH RISK (4 tests)
    ├─ Permission boundaries
    ├─ Side effect atomicity
    └─ Response shape contracts

🟢 STANDARD (8 tests)
    ├─ Sort order behaviors
    ├─ Soft delete behaviors
    └─ API consistency
```

## Monitoring Behavior Lock Health

```
Week 1:  ████████████████ 16/16 Pass ✅
Week 2:  ████████████████ 16/16 Pass ✅
Week 3:  ███████████████░ 15/16 Pass ⚠️  ◄── Investigate!
Week 4:  ████████████████ 16/16 Pass ✅
```

Track:
- Pass rate over time
- Which behaviors fail most often
- Time to fix failures
- Root cause of failures

---

**Remember:** Behavior locks are your safety net against silent regressions. Keep them green! 🟢
